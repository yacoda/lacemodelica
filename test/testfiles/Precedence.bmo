//! base 0.1.0
package 'Precedence'
  model 'Precedence' "Operator precedence and grouping"
    Real 'x'(start = 1.0);
    Real 'y'(start = 2.0);
    Real 'z';
    Real 'w';
    Real 'v';
  equation
    der('x') = 0.1;
    der('y') = -0.2;
    'z' = 'x' + 'y' * 2.0;
    'w' = ('x' + 'y') * 2.0;
    'v' = ('x' * 'y') / (('x' + 1.0) * ('y' + 1.0));
  end 'Precedence';
end 'Precedence';
